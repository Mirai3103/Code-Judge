using Code_Judge.Application.Common.Exceptions;
using Code_Judge.Application.TodoLists.Commands.CreateTodoList;
using Code_Judge.Application.TodoLists.Commands.DeleteTodoList;
using Code_Judge.Domain.Entities;
using FluentAssertions;
using NUnit.Framework;

namespace Code_Judge.Application.IntegrationTests.TodoLists.Commands;

using static Testing;

public class DeleteTodoListTests : BaseTestFixture
{
    [Test]
    public async Task ShouldRequireValidTodoListId()
    {
        var command = new DeleteTodoListCommand(99);
        await FluentActions.Invoking(() => SendAsync(command)).Should().ThrowAsync<NotFoundException>();
    }

    [Test]
    public async Task ShouldDeleteTodoList()
    {
        var listId = await SendAsync(new CreateTodoListCommand
        {
            Title = "New List"
        });

        await SendAsync(new DeleteTodoListCommand(listId));

        var list = await FindAsync<TodoList>(listId);

        list.Should().BeNull();
    }
}
